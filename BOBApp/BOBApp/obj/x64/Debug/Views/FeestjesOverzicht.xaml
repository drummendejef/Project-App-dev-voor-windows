<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BOBApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Maps="using:Windows.UI.Xaml.Controls.Maps"
    x:Class="BOBApp.Views.FeestjesOverzicht"
    mc:Ignorable="d"
    DataContext="{Binding FeestjesOverzichtVM, Mode=TwoWay, Source={StaticResource Locator}}">
    

    <Page.Resources>
        <!--Dit moet in commentaar omdat ik anders die datatemplate niet kan aanmaken? Geen idee hoe dat op te lossen, en tbh is dat niet een van m'n grootste zorgen atm, Joren-->
        <!--<ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ms-appx:///Styles/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>-->
        <DataTemplate x:Key="DTemplateFeestjesMap">
            <ContentControl Maps:MapControl.Location="{Binding GeoLocation}">
                <Grid Background="#3e5d94" Margin="40,0,0,0" Padding="5">
                    <TextBlock Text="Feest: " Margin="0" />
                    <TextBlock Text="{Binding Name}" Margin="85,0,0,0" />
                    <TextBlock Text="Organisator:" Margin="0,20,0,0"/>
                    <TextBlock Text="{Binding Organisator}" Margin="85,20,0,0"/>
                    <TextBlock Text="Aanwezig: " Margin="0,40,0,0"/>
                    <TextBlock Text="{Binding Amount}" Margin="85,40,0,0"/>
                    <!--<Button Content="Facebook(doetnogniets)" Click="" Margin="0,60,0,0"/>-->
                    <HyperlinkButton Content="Facebook" NavigateUri="{Binding FacebookLinkID}" Margin="0,60,0,0" />
                </Grid>
            </ContentControl>
        </DataTemplate>
    </Page.Resources>

    <RelativePanel x:ConnectionId='1' x:Name="Root">
        <!--Kan hier geen stijl aan meegeven, zie commentaar hier boven-->
        <TextBox x:ConnectionId='2' x:Name="textBoxZoek" Height="32" PlaceholderText="Zoek locatie/feestje" Text="{Binding ZoekVeld, Mode=TwoWay}" VerticalAlignment="Top" RelativePanel.AlignHorizontalCenterWithPanel="True" Margin="10" RelativePanel.AlignRightWithPanel="True" RelativePanel.AlignLeftWithPanel="True"/>
        <!--<TextBox x:Name="textBoxZoek" Height="32" PlaceholderText="Zoek locatie/feestje" Text="{Binding ZoekVeld, Mode=TwoWay}" VerticalAlignment="Top" RelativePanel.AlignHorizontalCenterWithPanel="True" Margin="10" RelativePanel.AlignRightWithPanel="True" RelativePanel.AlignLeftWithPanel="True" Style="{StaticResource textBox}"/>-->

        <Maps:MapControl x:ConnectionId='3' x:Name="MapFeestOverzicht" MapServiceToken="dOUBDBVwN5QvZ1iHg90c~s2bgtqxiAZX20yceA6JFuw~An9qrmMutNOdQJ0PiF_t7WMqjN4lZBOWQaKrphjthrGdwmqvhjUvX8--_O2kP2K5" RelativePanel.AlignRightWithPanel="True" RelativePanel.AlignLeftWithPanel="True" RelativePanel.Below="textBoxZoek" RelativePanel.AlignBottomWithPanel="True" Margin="10">
            <Maps:MapItemsControl ItemsSource="{Binding Feestjes}" ItemTemplate="{StaticResource DTemplateFeestjesMap}" />
        </Maps:MapControl>

    </RelativePanel>
</Page>

