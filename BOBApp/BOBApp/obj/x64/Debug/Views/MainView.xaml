<Page
    x:Class="BOBApp.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BOBApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
     DataContext="{Binding MainViewVM, Mode=TwoWay, Source={StaticResource Locator}}">

    <Page.Resources>
        <ResourceDictionary Source="ms-appx:///Styles/SplitView.xaml">
            <ResourceDictionary.MergedDictionaries>

            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>
    <SplitView x:ConnectionId='1' x:Name="ShellSplitView" DisplayMode="CompactOverlay"
               IsPaneOpen="False" CompactPaneLength="48" OpenPaneLength="200"
               PaneBackground="{ThemeResource SplitViewBackgroundBrush}">
        <SplitView.Pane>
            <StackPanel>
                <!-- Menu (hamburger) button -->
                <RadioButton x:ConnectionId='2' Style="{StaticResource SplitViewMenuButtonStyle}"                             />

                <!-- Home button -->
                <RadioButton x:ConnectionId='3' Tag="&#xe10f;" Content="Home" Style="{StaticResource SplitViewNavButtonStyle}"                               IsChecked="True" />

                <!-- Search button -->
                <RadioButton x:ConnectionId='4' Tag="&#xe11a;" Content="Search" Style="{StaticResource SplitViewNavButtonStyle}"                                 />

                <!-- Settings button -->
                <RadioButton x:ConnectionId='5' Tag="&#xe115;" Content="Settings" Style="{StaticResource SplitViewNavButtonStyle}"                                   />

                <!-- About button -->
                <RadioButton x:ConnectionId='6' Tag="&#xe783;" Content="About" Style="{StaticResource SplitViewNavButtonStyle}"                                />
            </StackPanel>
        </SplitView.Pane>
        <SplitView.Content>
            <Frame x:ConnectionId='7' x:Name="frame"/>
        </SplitView.Content>
    </SplitView>
</Page>

