<Page
    x:Class="BOBApp.Views.Bestemmingen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BOBApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="DatTemplate">
            <StackPanel HorizontalAlignment="Stretch">

            </StackPanel>
        </DataTemplate>
    </Page.Resources>

    <RelativePanel>
        <TextBox x:Name="textBoxZoekLocaties" Height="32" TextWrapping="Wrap" Text="" PlaceholderText="Locatie" Margin="10" RelativePanel.AlignRightWithPanel="True" RelativePanel.AlignLeftWithPanel="True" />
        <ItemsControl x:Name="ItemsControlBestemmingen" Margin="10" RelativePanel.Below="textBoxZoekLocaties" RelativePanel.AlignBottomWithPanel="True" RelativePanel.AlignLeftWithPanel="True" RelativePanel.AlignRightWithPanel="True" ItemsSource="{Binding BestemmingenLijst}" ItemTemplate="{StaticResource DatTemplate}"/>
    </RelativePanel>
</Page>
